require "spec_helper"

describe NaturalLangDateParser do
  it "has a version number" do
    expect(NaturalLangDateParser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
